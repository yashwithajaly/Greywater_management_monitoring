name: greyappnew
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0 # Updated to the latest version

  fl_chart: ^0.70.2 # Updated to the latest version



  flutter_local_notifications: ^19.0.0 # Updated to the latest version



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
